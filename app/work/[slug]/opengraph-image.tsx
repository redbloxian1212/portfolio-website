// Case studies share the site's preview card. Without this file, the
// page-level openGraph title in page.tsx would drop the image from the root.
export { default, alt, size, contentType } from "@/app/opengraph-image";
